{
    "projectName": "Template",
    "baseURL": "{{url}}",
    "headers": [
        {
            "key": "x-session-id",
            "type": "text",
            "value": "{{x-session-id}}"
        },
        {
            "key": "x-tid",
            "type": "text",
            "value": "{{x-tid}}"
        }
    ]
}