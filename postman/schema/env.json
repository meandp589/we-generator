{
    "idForEvent": "$(cmd)_id",
    "projectName": "Template",
    "baseURL": "{{url}}",
    "headers": [
        {
            "key": "x-session-id",
            "type": "text",
            "value": "{{x-session-id}}"
        },
        {
            "key": "x-tid",
            "type": "text",
            "value": "{{x-tid}}"
        }
    ],
    "auth": {
        "type": "bearer",
        "bearer": [
            {
                "key": "token",
                "value": "{{access_token}}",
                "type": "string"
            }
        ]
    }
}